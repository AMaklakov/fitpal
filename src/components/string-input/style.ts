import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	inputWrapper: {
		borderWidth: 1,
		borderColor: 'black',
		borderRadius: 15,
	},
	input: {
		color: 'black',
		padding: 10,
		fontSize: 16,
	},
});

export default style;
