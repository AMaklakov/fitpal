import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	inputWrapper: {
		padding: 10,
		margin: 5,

		borderWidth: 1,
		borderColor: 'black',
		borderRadius: 15,
	},
});

export default style;
