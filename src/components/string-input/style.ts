import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	inputWrapper: {
		borderWidth: 1,
		borderColor: 'black',
		borderRadius: 15,
	},
	input: {
		color: 'black',
	},
});

export default style;
