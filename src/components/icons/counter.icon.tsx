import React from 'react';
import { IconProps } from '@icons/model/icon-props.type';
import { MaterialCommunityIcon } from '@icons/material-community-icon';

export const CounterIcon = (props: IconProps) => <MaterialCommunityIcon name="counter" {...props} />;
