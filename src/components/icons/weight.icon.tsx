import React from 'react';
import { IconProps } from '@icons/model/icon-props.type';
import { FontAwesomeIcon } from '@icons/font-awesome-5';

export const WeightIcon = (props: IconProps) => <FontAwesomeIcon name="weight-hanging" {...props} />;
