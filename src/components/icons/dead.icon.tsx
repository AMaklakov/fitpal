import React from 'react';
import { IconProps } from '@icons/model/icon-props.type';
import { BaseMaterialIcon } from '@icons/base-material-icon';

// TODO find a more suitable one
export const DeadIcon = (props: IconProps) => <BaseMaterialIcon name="sentiment-very-dissatisfied" {...props} />;
